# ===========================================
# PUBLI*CAT - Variables d'Entorn
# ===========================================
# Copia aquest fitxer a .env.local i omple els valors
# cp .env.example .env.local

# -------------------------------------------
# Supabase
# -------------------------------------------
# URL del projecte Supabase (Dashboard > Settings > API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Clau anònima (pública) - Dashboard > Settings > API > anon public
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Clau de servei (SECRETA) - Dashboard > Settings > API > service_role
# IMPORTANT: Aquesta clau bypassa RLS, mai exposar al client!
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Connexió directa a PostgreSQL (opcional, per MCP)
DATABASE_URL=postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres

# -------------------------------------------
# Vimeo
# -------------------------------------------
# Token d'accés de Vimeo (https://developer.vimeo.com/apps)
# Scopes necessaris: private, upload, video_files, public
VIMEO_ACCESS_TOKEN=your-vimeo-access-token

# -------------------------------------------
# Cron Jobs (Producció)
# -------------------------------------------
# Secret per protegir els endpoints de cron
# Genera un valor aleatori: openssl rand -hex 32
CRON_SECRET=your-random-secret-for-cron

# -------------------------------------------
# Configuració de Vídeos
# -------------------------------------------
# Mida màxima de vídeo en MB (per defecte 2GB)
MAX_VIDEO_SIZE_MB=2048

# Formats de vídeo permesos
ALLOWED_VIDEO_FORMATS=mp4,mov,avi,mkv,webm

# Mida del chunk per pujar a Vimeo (MB)
VIMEO_UPLOAD_CHUNK_SIZE_MB=10
